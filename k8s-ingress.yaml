apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: jarvis-ingress
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  rules:
  - host: jarvis.local
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: jarvis-service
            port:
              number: 80