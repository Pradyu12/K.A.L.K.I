apiVersion: v1
kind: Service
metadata:
  name: jarvis-service
  labels:
    app: jarvis
spec:
  selector:
    app: jarvis
  ports:
  - protocol: TCP
    port: 80
    targetPort: 8000
  type: ClusterIP