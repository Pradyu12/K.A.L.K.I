{
	"version": "2.0.0",
	"tasks": [
		{
			"type": "docker-build",
			"label": "docker-build",
			"platform": "node",
			"dockerBuild": {
				"dockerfile": "${workspaceFolder}/.kilo/node_modules/@standard-schema/spec/Dockerfile",
				"context": "${workspaceFolder}/.kilo/node_modules/@standard-schema/spec",
				"pull": true
			},
			"node": {
				"package": "${workspaceFolder}/.kilo/node_modules/@standard-schema/spec/package.json"
			}
		},
		{
			"type": "docker-run",
			"label": "docker-run: release",
			"dependsOn": [
				"docker-build"
			],
			"platform": "node",
			"node": {
				"package": "${workspaceFolder}/.kilo/node_modules/@standard-schema/spec/package.json"
			}
		},
		{
			"type": "docker-run",
			"label": "docker-run: debug",
			"dependsOn": [
				"docker-build"
			],
			"dockerRun": {
				"env": {
					"DEBUG": "*",
					"NODE_ENV": "development"
				}
			},
			"node": {
				"package": "${workspaceFolder}/.kilo/node_modules/@standard-schema/spec/package.json",
				"enableDebugging": true
			}
		}
	]
}